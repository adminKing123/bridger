/* app/static/js/syncore.js
   Loaded on: syncore/index.html
   Init functions are no-ops if their target elements are absent. */

(function () {
  'use strict';

  document.addEventListener('DOMContentLoaded', function () {
    // placeholder — real interactions added when SynCore features ship
  });
}());
